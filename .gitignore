# Local tooling (portable postgres, data dirs)
.local/

# Go
server/kingsreach
server/kingsreach.exe
*.test

# Client
client/node_modules/

# Build output — regenerated by `npm run build` (and by the Dockerfile).
web/

# Unity (legacy frontend, see unity/README.md)
unity/Library/
unity/Temp/
unity/Logs/
unity/UserSettings/
unity/obj/
unity/Build/
unity-build/
unity/*.csproj
unity/*.sln

# OS noise
Thumbs.db
.DS_Store

# Claude Code keeps a per-machine permission allowlist here. It is full of
# absolute paths from whoever last worked on this checkout, and it is of no
# use to anyone else — it was scrubbed from the history once, so keep it out.
.claude/
